diff --git a/monitoring/files/docker-compose.yml b/monitoring/files/docker-compose.yml
index e59f1fd..1516c36 100644
--- a/monitoring/files/docker-compose.yml
+++ b/monitoring/files/docker-compose.yml
@@ -3,14 +3,14 @@ version: '3.7'
 services:
   grafana:
     image: grafana/grafana:8.4.4
-    # network_mode: host
+    network_mode: host
     logging:
       driver: "json-file"
       options:
         max-size: "20m"
         max-file: "10"
     networks:
-      - monitoring
+      - host
     volumes:
       - /etc/tvm/grafana:/var/lib/grafana
       - /etc/tvm/grafana-provisioning/:/etc/grafana/provisioning
@@ -20,14 +20,14 @@ services:
 
   loki:
     image: grafana/loki:main-52f9df4
-    # network_mode: host
+    network_mode: host
     logging:
       driver: "json-file"
       options:
         max-size: "20m"
         max-file: "10"
     networks:
-      - monitoring
+      - host
     volumes:
       - /etc/tvm/loki-config.yml:/etc/loki/loki-config.yaml
       - /etc/tvm/loki_data:/data/loki
@@ -35,14 +35,14 @@ services:
 
   postgres:
     image: postgres:12.10
-    # network_mode: host
+    network_mode: host
     logging:
       driver: "json-file"
       options:
         max-size: "20m"
         max-file: "10"
     networks:
-      - monitoring
+      - host
     environment:
       - POSTGRES_USER={{ passwords.postgres_user }}
       - POSTGRES_PASSWORD={{ passwords.postgres_password }}
@@ -63,7 +63,7 @@ services:
     # ports:
     #   - 9090:9090
     networks:
-      - monitoring
+      - host
 
   fetcher:
     image: jenkins_fetcher:latest
@@ -75,33 +75,33 @@ services:
     volumes:
       - /etc/tvm/fetcher_data:/opt/fetcher/.httpcache
     networks:
-      - monitoring
+      - host
     depends_on:
       - postgres
 
   nginx:
     image: nginx:1.21.0
-    # network_mode: host
-    ports:
-      - "80:80"
-      - "443:443"
+    network_mode: host
+    # ports:
+    #   - "80:80"
+    #   - "443:443"
     logging:
       driver: "json-file"
       options:
         max-size: "20m"
         max-file: "10"
     networks:
-      - monitoring
+      - host
     volumes:
       - "/etc/tvm/http.conf:/etc/nginx/conf.d/default.conf"
       - "/etc/tvm/certificate.pem:/etc/nginx/fullchain.pem"
       - "/etc/tvm/key.pem:/etc/nginx/privkey.pem"
 
-networks:
-  monitoring:
-    external: false
-
 # networks:
-#   host:
-#     name: host
-#     external: true
\ No newline at end of file
+#   monitoring:
+#     external: false
+
+networks:
+  host:
+    name: host
+    external: true
\ No newline at end of file
